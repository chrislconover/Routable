# Routable
Simple composable screen routing for UIKit.

Inspired by routing in web frameworks, `Routable` allows simple push / pop / present / root (replace) operations to navigate between screens in UIKit. Allows deep linking through a single call with chained / nested screens to place the user inside a hierarchy, including modal screens.
